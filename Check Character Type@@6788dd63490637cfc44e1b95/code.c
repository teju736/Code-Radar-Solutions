#include <stdio.h>
#include <ctype.h>
int main(){
    char i;
    scanf("%c",&i);
    if(isdigit(i)){
        printf("Digit");
    }
     ch = tolower(ch);
        if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
            printf("Vowel\n");
        } else {
            printf("Consonant\n");
        }
    }
    else {
        printf("Special Character\n");
    }

    

